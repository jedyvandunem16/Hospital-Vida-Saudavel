# ================================
#  Hospital Vida Saudável — .env
# ================================

# Servidor
PORT=
NODE_ENV=

# Base de Dados MySQL
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# JWT (muda para uma string longa e aleatória em produção)
JWT_SECRET=
JWT_EXPIRES_IN=

# Email (Nodemailer — SMTP)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=""

# Frontend URL (para CORS)
FRONTEND_URL=

# Admin padrão (criado no primeiro setup)
ADMIN_NOME=
ADMIN_EMAIL=
ADMIN_PASSWORD=
